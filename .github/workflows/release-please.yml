name: Call Release Please

on:
  push:
    branches:
      - master

jobs:
  tf-tests:
    uses: entur/tmp-gh-workflows-2/.github/workflows/release-please.yml@main
    with:
      release_type: terraform-module
      version_bumped_files: |
        README.md
        examples/init/main.tf
